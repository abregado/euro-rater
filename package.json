{
  "name": "euro-rater-root",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "start:server": "node server/server.js",
    "dev:client": "npm run dev --prefix client"
  }
}
